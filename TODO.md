- Write tests
- Parse the response (instead of just printing it)
- Implement multiple methods
- Handle DNS
- Handle redirects
- TLS/HTTPS (this one is harder, keep it for the end)

Some other ideas:
- Implement my own url parser (instead of using std.Uri)
